<div>
    <h1>All users</h1>
    <% @users.each do |user| %>
        <div>
            <h2><%= user.name %></h2>
            <p><%= user.email %></p>
            <p><%= image_tag avatar_url_for(user, size: 32) %></p>
        </div>
    <% end %>
</div>